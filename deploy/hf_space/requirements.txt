# Dependencias del servicio de clasificacion radiologica (Hugging Face Spaces)
# Solo lo necesario para inferencia + Grad-CAM. El entrenamiento no se despliega.

--extra-index-url https://download.pytorch.org/whl/cpu

# --- Motor de deep learning (CPU) ---
# torch es imprescindible: Grad-CAM necesita el motor de autograd para
# retropropagar el logit de la clase predicha.
torch==2.9.1+cpu
torchvision==0.24.1+cpu

# --- Modelo de vision ---
ultralytics==8.3.230

# --- Vision por computador ---
# Se usa la variante headless: no requiere libGL ni servidor grafico.
opencv-python-headless==4.12.0.88

# --- Servidor de API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.0,<3.0

# --- Utilidades base ---
numpy==2.2.6
